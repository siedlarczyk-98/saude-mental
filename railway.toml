[build]
buildCommand = "npm install && npm run build -w @saude-mental/shared && npm run build -w @saude-mental/api"

[deploy]
startCommand = "node apps/api/dist/server.js"
healthcheckPath = "/health"
